try:
    age = int(input("🎂 Tell your age: "))

    if age < 0:
        raise ValueError("Age cannot be negative.")
    elif age < 18:
        raise ValueError("You must be 18 or older to join the party.")
    else:
        print("🎉 Welcome to the party! Enjoy yourself! 🥳")

except ValueError as err:
    print(f"⚠️ Error: {err}")
    print("⏳ Please try again when you meet the age requirement.")


    
